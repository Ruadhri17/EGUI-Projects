import { Navbar } from 'react-bootstrap';

export const NavBar = () => (
    <Navbar bg="dark" variant="dark" expand="lg">
        <Navbar.Brand href="#">EGUI - Reciper v1.0</Navbar.Brand>
    </Navbar>
)